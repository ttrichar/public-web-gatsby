{
  "name": "@fathym-it/public-web-gatsby",
  "version": "0.0.0",
  "private": true,
  "description": "My Gatsby Site",
  "author": "mcgear",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "develop": "gatsby develop",
    "start": "gatsby develop",
    "build": "gatsby build",
    "deploy": "npm version patch && npm run deploy:app",
    "deploy:app": "npm version patch --prefix static && npm run build && npm publish ./public --access public",
    "serve": "gatsby serve",
    "clean": "gatsby clean"
  },
  "dependencies": {
    "gatsby": "^4.7.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  }
}
